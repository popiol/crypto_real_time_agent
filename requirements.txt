# Core
numpy>=2.0
psutil>=5.0
pydantic>=2.0
PyYAML>=6.0
requests>=2.0
tensorflow>=2.0
scikit-learn>=1.9
pandas>=3.0

# LLM — install the package matching your configured llm_model prefix
langchain-core>=0.3
langchain-google-genai>=2.0   # gemini-*
langchain-anthropic>=0.3      # claude-*
langchain-openai>=0.2         # gpt-* / o1 / o3 / o4
